import { Dimensions } from 'react-native'

const { width, height } = Dimensions.get('screen')

const BORDER_RADIUS = 12
const BORDER_RADIUS_SM = 8

const GAP = 12
const GAP_SM = 8
const GAP_XS = 6

export { width, height, BORDER_RADIUS, BORDER_RADIUS_SM, GAP, GAP_SM, GAP_XS }
